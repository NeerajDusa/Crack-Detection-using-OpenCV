Installation:

pip install -r requirements.txt
pip install streamlit
streamlit hello

To run the program :

streamlit app.py  

then open the link shown in the terminal to view the web application